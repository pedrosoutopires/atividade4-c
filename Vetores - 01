#include <stdio.h>

int main(){

    int i;
    float v[4], soma = 0;

    for (i=0; i<4; i++){
        scanf("%f", &v[i]);
        soma += v[i];
    }

    for (i=0; i<4; i++){
        printf("Nota %d: %.2f\n", i+1, v[i]);
    }

    printf("A media e: %.2f", soma/4);

    return 0;
}
