#include <stdio.h>

int main(){

    int i, v[10], maior, posicao, soma = 0;

    printf("Digite um numero inteiro: ");
    scanf("%d", &v[0]);
    maior = v[0];
    soma = v[0];
    posicao = 0;

    for(i=1; i<10; i++){
        printf("Digite um numero inteiro: ");
        scanf("%d", &v[i]);
        soma += v[i];

        if (v[i] > maior){
            maior = v[i];
            posicao = i;
        }
    }
    printf("Maior: %d\nPosicao: %d\nSoma: %d", maior, posicao, soma);

    return 0;
}
